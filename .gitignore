# Build output
src/bin/
src/obj/

# Decompiled source (large, regenerable from game DLL)
decompiled_full/

# Game assemblies (copyrighted, not distributable)
libs/

# OS/IDE files
.vs/
*.user
*.suo
Thumbs.db
Desktop.ini

# NuGet
*.nupkg

# All types listing (regenerable)
all_types.txt

# Claude session tooling (local process commands)
.claude/

# Design docs — local working artifacts, not for the public repo
# (owner ruling 2026-08-18: the published repo carries mod code,
# dependencies, and licenses only)
docs/combat-survey.md
docs/combat-spec.md
docs/text-surface-audit.md
docs/table-ui-design.md

# Dev bridge — local-only, never ships (lineage rule)
bridge/bin/
bridge/obj/

# Session log snapshots (bridge log watcher, 2026-08-18)
logs/

# Reception analytics history (gha) — local trend data, not for the public repo
docs/reception_log.jsonl

# Tile-art classification working set — game-derived data (sprite censuses,
# label tables, the review page with embedded game sprites) and player
# feedback transcripts; NEVER for the public repo (owner ruling 2026-09-01).
# The derived speech vocabulary ships only as compiled-in mod source
# (src/TileArtData.cs / src/TileArtTable.cs).
docs/tile-art/
docs/tile-art-review.html
